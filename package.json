{
  "dependencies": {
    "@vercel/analytics": "1.5.0",
    "@vercel/speed-insights": "1.2.0",
    "parcel": "2.15.2"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir dist && node scripts/generate-sitemap.js"
  },
  "devDependencies": {
    "sitemap": "8.0.0",
    "svgo": "3.3.2"
  }
}
