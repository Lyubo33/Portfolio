{
  "dependencies": {
    "@vercel/analytics": "1.5.0",
    "@vercel/speed-insights": "1.2.0",
    "parcel": "^2.14.4"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir dist && node scripts/generate-sitemap.js && node scripts/optimize-images.js"
  },
  "devDependencies": {
    "sitemap": "^8.0.0",
    "svgo": "^3.3.2"
  }
}
